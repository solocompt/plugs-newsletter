============
Installation
============

At the command line::

    $ easy_install plugs-newsletter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-newsletter
    $ pip install plugs-newsletter
